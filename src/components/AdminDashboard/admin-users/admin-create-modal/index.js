import React, { useState } from "react";

const AdminCreateModal = ({ keys }) => {
  const [item, setItem] = useState(keys);
  return <div>AdminCreateModal</div>;
};

export default AdminCreateModal;
